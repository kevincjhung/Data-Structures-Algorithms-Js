const { AdjacencyMatrixGraph } = require('../10-depthFirstSearch/AdjacencyMatrixGraph');


describe('AdjacencyMatrixGraph', () => {
  let graph;
  
  beforeEach(() => {
    graph = new AdjacencyMatrixGraph();
  })

  describe('To Be Completed', () => {
    it('to be completed', () => {
    
      expect(1).toBe(1)
    });    
  });
})
